# Hermes Agent 子ども向けチャットUI — 設計ハンドオフ

作成日: 2026-09-12 / 実装は Claude Code で行う前提のドキュメント。
リポジトリ名（仮）: `hermes-kids-chat`

---

## 1. 何を作るか

Discordなどのチャットツールを使えない子どもたちが、URLひとつで Hermes Agent と会話できるWebチャットを作る。アカウント登録はなし、入室時に名前を入れるだけ。複数人が同じ部屋に入って、一緒にエージェントに話しかける。

満たすべき要件は以下。

- 基本のUIはチャット
- 複数ユーザーが同じ部屋に参加できる
- アカウント不要
- URLを知っている人しか入れない
- 部屋URLの作成・削除ができる
- 入室時に名前を入力する
- 画像などのファイル添付ができる

---

## 2. Hermes側の前提（確認済みの事実）

公式ドキュメント（hermes-agent.nousresearch.com/docs）で確認した内容。

**接続口は API Server。** ゲートウェイの設定で `API_SERVER_ENABLED=true` にすると、OpenAI互換のHTTPサーバーが立つ。

| 設定 | 既定値 | 意味 |
|---|---|---|
| `API_SERVER_ENABLED` | `false` | API server を有効化 |
| `API_SERVER_PORT` | `8642` | ポート |
| `API_SERVER_HOST` | `127.0.0.1` | バインドアドレス |
| `API_SERVER_KEY` | （必須） | Bearerトークン |

叩くエンドポイントは `POST /v1/chat/completions`（OpenAI互換）。`/health` と `/v1/models` で疎通確認できる。モデル名は既定でプロファイル名（デフォルトなら `hermes-agent`）。

**会話履歴はクライアントが毎回まるごと送る。** Open WebUIも同じ方式で動いている。サーバー側に会話状態を持たせる `/v1/responses` もあるが実験的。毎ターン履歴を組み立てて送る方式なら、「部屋の会話ログは自前で持つ」という設計と素直に噛み合う。複数人が1つの部屋で話す用途にはこちらが向く。

**ツールはAPIサーバーのホスト上で実行される。** API Server のツールセット `hermes-api-server` は `clarify` と `text_to_speech` を除くフルツールで、terminal を含む。つまり素のままだと、URLを知っている子ども全員がそのサーバーでコマンドを実行できる状態になる。

**Hermesの信頼モデルは「操作者は1人」。** 公式のセキュリティ方針に、Hermesが守るのは「LLMの行動から操作者を守ること」であり、悪意ある同居ユーザー同士の分離は想定外、マルチユーザーの分離はOS/ホスト側でやること、と明記されている。ゲートウェイの呼び出し元はすべて同じ信頼度として扱われ、セッションキーは認可の境界ではなくルーティング用。**この一文が本設計のいちばん重い制約**で、以下の判断はすべてここから出ている。

**terminalの実行環境は差し替えられる。** `terminal.backend` に `local`（既定）/ `docker` / `modal` / `daytona` などを指定できる。コンテナ系バックエンドでは、危険コマンドの承認フローを経ずにコンテナ境界そのものを安全境界として扱う。Dockerの場合は `--cap-drop ALL`、`--security-opt no-new-privileges` 等のハードニングが既定で入る。`container_persistent: false` にするとセッションごとに使い捨てのコンテナになる。

---

## 3. 全体構成

ブラウザからHermesを直接叩かせない。あいだに自前のサーバー（BFF）を必ず挟む。

```
 子どものブラウザ
      │  HTTPS（部屋URL・SSE・ファイルアップロード）
      ▼
 ┌─────────────────────────────┐
 │ BFF (Node/TypeScript)       │  ← 唯一の信頼境界
 │  ・部屋と参加者の管理        │
 │  ・会話ログの保持            │
 │  ・添付ファイルの保管        │
 │  ・レート制限/ターン上限      │
 │  ・APIキーの保持             │
 └─────────────────────────────┘
      │  HTTP（127.0.0.1:8642, Bearer）
      ▼
 ┌─────────────────────────────┐
 │ Hermes Agent (profile: kids)│
 │  terminal.backend: docker   │ → ツールは使い捨てコンテナの中
 └─────────────────────────────┘
      │  HTTPS
      ▼
 さくらのAI Engine (https://api.ai.sakura.ad.jp/v1)
```

BFFを挟む理由は3つ。APIキーをブラウザに出さないこと、部屋という概念がHermes側に存在しないこと、そしてHermesが「1人の操作者」を前提にしている以上、誰が何をどれだけ投げられるかを制御する層が外側に必要なこと。

### 3.1 運用の前提 — 月1回のイベント単位で立てて畳む

使うのは月に1回程度。常時稼働のサービスではない。この前提が以下の判断すべてを支えている。

- サーバーのプランはイベント前に上げ、終わったら戻す（10.1）
- Hermesのプロファイルはイベントごとに作り直す。子どもたちの会話が長期記憶に残り続けない
- 部屋は当日作って当日消す
- 常時動かすのはBFFだけ。それも必須ではなく、前日に起動する運用でよい

つまり「止まっている時間のほうが長いシステム」として設計する。起動手順をMakefileかシェルスクリプトに落とし、当日の作業を数コマンドで済ませられる状態にしておくこと。手順が頭の中にしかないと、月1回では確実に忘れる。

---

## 4. 設計判断とその理由

### 4.1 エージェントの権限 — Docker隔離のうえでフルツールを残す

terminalを外して「会話＋Web検索だけ」にする案も検討したが、採らない。子どもにとっていちばん面白いのは、頼んだものを実際に作って動かして見せてくれるところで、そこを削ると普通のチャットボットになる。

代わりにホストとの境界を物理的に引く。

```yaml
# ~/.hermes/profiles/kids/config.yaml
terminal:
  backend: docker
  container_persistent: false   # 使い捨て
  container_cpu: 1
  container_memory: 1024
  container_disk: 8192          # 既定の51200(50GB)はディスク20GBに対して過大
```

あわせて次を守る。

- 子ども用は専用プロファイル（`hermes -p kids`）にする。個人利用のプロファイルと記憶・スキル・認証情報を混ぜない
- `API_SERVER_HOST` は `127.0.0.1` のまま。外部に公開せず、BFF経由でしか到達できない状態を保つ
- `terminal.docker_forward_env` には何も足さない。コンテナに渡した環境変数はコンテナ内のコードから読める
- ブラウザ自動操作（Chromium）系のツールは無効にする。メモリを数百MB単位で食ううえ、子どもに任意のサイトを開かせる経路にもなる
- SOUL.md / system_prompt で子ども向けの口調と話題の制限を書く。ただしこれは体験の調整であって安全装置ではない。**プロンプトで書いた禁止事項は破られる前提で設計する**

残るリスクは正直に書いておく。コンテナの中とはいえ外向きの通信はできるので、子どもがエージェントに頼んで外部サイトに何かを送ることは原理的に可能。イベント単位でプロファイルごと作り直し、終わったら消す運用を推奨する。

### 4.2 部屋でのエージェントの振る舞い — 文脈は全員分、返答は呼びかけ時のみ

3案のうち、「全員の発言を見るが、呼びかけられたときだけ返す」を既定にする。

参加者ごとに会話を分ける案は、同じ部屋にいる意味が薄れるので採らない。子どもたちが互いの質問とエージェントの答えを見られることが、この場のいちばんの価値になる。

一方で、5人も10人もいる部屋で全発言に返すと、会話が読めない速さで流れるしAPIコストも跳ねる。だから履歴には全員の発言を入れ、発火だけ絞る。

呼びかけの判定は2系統。

1. 入力欄の横の「AIにきく」ボタン（既定の導線）。`@` の入力は低学年には確実でないので、ボタンを主役にする
2. 本文が `@ヘルメス` などのメンションで始まる場合

部屋の設定で「毎回返す」モードにも切り替えられるようにする（少人数のときはこちらが楽しい）。

エージェントに送る履歴では、発言者を本文の先頭に付ける。

```
user: たろう: このプログラムなんでうごかないの？
user: はなこ: わたしもおなじところでつまった
user: たろう: @ヘルメス おしえて
```

system prompt側で「複数の子どもが同じ部屋にいる」「名前付きで発言が届く」「返すときは誰に向けた返事か分かるように書く」と説明しておく。

### 4.3 URLの秘匿性だけに頼りすぎない

要件は「URLを知っている人しか入れない」だが、子どもがURLをそのままSNSに貼る可能性は普通にある。以下を既定で入れる。

- 部屋IDは推測不能な22文字程度のランダム文字列（`crypto.randomUUID()` ではなく base62 の乱数で短く）
- 部屋には有効期限を持たせる（既定4時間、管理画面で延長可）
- 任意で4桁の合言葉を設定できる（当日その場で口頭で伝える運用）
- `Referrer-Policy: no-referrer` と `X-Robots-Tag: noindex` を付ける
- 定員を設ける（既定20人）。超えたら入室を断る

---

## 5. 機能仕様

### 5.1 部屋

| 項目 | 内容 |
|---|---|
| 作成 | 管理画面から。部屋名、有効期限、定員、合言葉の有無、返答モードを指定 |
| URL | `/r/{roomId}` |
| 削除 | 管理画面から。削除時は会話ログをJSONでエクスポートしてから論理削除 |
| 期限切れ | 入室不可。既に入室中の人には「この部屋はおわりました」を表示 |

### 5.2 入室

名前を入力して入る。パスワードもメールもなし。

- 名前は部屋内で一意。重複したら「その名前はもう使われているよ」と伝えて再入力させる
- 入室時にBFFが参加者トークンを発行し、HttpOnly cookieに置く。以降の発言はこのトークンで本人性を担保する（完全ななりすまし防止ではないが、同じ部屋の中で名前を乗っ取られることは防げる）
- 名前は1〜12文字、絵文字と記号は許可、改行とタブは除去
- 「ここでの会話は大人が見られるように記録されます」と入室画面に明示する

### 5.3 メッセージ

- 種別は `user`（子ども）/ `agent`（Hermes）/ `system`（入退室、部屋の開始終了）の3つ
- エージェントの応答はストリーミングで全員の画面に流れる
- エージェントがツールを使っている間は「いま調べているよ」「プログラムを動かしているよ」のような日本語の状態表示に変換して見せる（Hermesはツール進捗をストリームに混ぜて返す）
- 誰でも押せる「とめる」ボタンを置く。押したら進行中のターンを打ち切る

### 5.4 添付ファイル

第1段階では、扱いを2段に分ける。

- **画像**（png/jpeg/webp/gif、10MBまで）: BFFが長辺1568pxに縮小し、部屋のタイムラインにサムネイル表示する。エージェントに渡せるかは、さくらのAI Engineの画像入力対応しだい（8.1）。非対応なら、この段では画像もファイル名だけを伝える扱いにする
- **画像以外**（pdf/txt/csv/zipなど）: 部屋の中で共有表示するだけ。エージェントには「〇〇.pdf が共有されました」とファイル名のみ伝える

画像以外をエージェントに読ませるには、ファイルをHermes側のコンテナに渡す経路が要る（`container_persistent: false` だとセッションごとに作り直されるため、単純な共有ディレクトリのマウントでは足りない可能性がある）。ここは実装時に検証してから第2段階で対応する。

拡張子はallowlist方式。実行可能形式は受け付けない。

### 5.5 管理画面

`/admin` に置き、環境変数の管理トークンで入る（子ども用の部屋URLとは完全に別系統）。

- 部屋の作成・一覧・削除
- 部屋ごとの会話ログ閲覧とエクスポート
- 参加者の強制退出
- 部屋ごとのターン使用量表示

---

## 6. データモデル

SQLiteで持つ。テーブルは4つ。

```sql
rooms(
  id TEXT PRIMARY KEY,          -- 22文字のランダム
  name TEXT NOT NULL,
  passcode_hash TEXT,           -- NULL可
  reply_mode TEXT NOT NULL,     -- 'mention' | 'always'
  capacity INTEGER NOT NULL,
  turn_limit INTEGER NOT NULL,  -- 1部屋あたりの総ターン上限
  turns_used INTEGER NOT NULL,
  expires_at TEXT NOT NULL,
  deleted_at TEXT,
  created_at TEXT NOT NULL
)

participants(
  id TEXT PRIMARY KEY,
  room_id TEXT NOT NULL,
  display_name TEXT NOT NULL,
  token_hash TEXT NOT NULL,
  joined_at TEXT NOT NULL,
  left_at TEXT,
  UNIQUE(room_id, display_name)
)

messages(
  id TEXT PRIMARY KEY,
  room_id TEXT NOT NULL,
  kind TEXT NOT NULL,           -- 'user' | 'agent' | 'system'
  participant_id TEXT,          -- kind='user' のとき
  body TEXT NOT NULL,
  created_at TEXT NOT NULL
)

attachments(
  id TEXT PRIMARY KEY,
  message_id TEXT NOT NULL,
  original_name TEXT NOT NULL,
  mime_type TEXT NOT NULL,
  size INTEGER NOT NULL,
  stored_path TEXT NOT NULL
)
```

MySQLも使える環境だが、この規模ならSQLiteで十分で、部屋ごとのエクスポートもバックアップも楽になる。

---

## 7. BFFのAPI

| メソッド | パス | 用途 | 認証 |
|---|---|---|---|
| `GET` | `/api/rooms/:id` | 部屋の存在確認（名前・期限・合言葉要否） | なし |
| `POST` | `/api/rooms/:id/join` | 名前を送って入室、参加者トークン発行 | なし |
| `GET` | `/api/rooms/:id/messages` | 入室直後の履歴取得 | 参加者トークン |
| `GET` | `/api/rooms/:id/stream` | SSE。新着メッセージとエージェントのストリームを配信 | 参加者トークン |
| `POST` | `/api/rooms/:id/messages` | 発言（`askAgent: boolean` を含む） | 参加者トークン |
| `POST` | `/api/rooms/:id/attachments` | ファイルアップロード | 参加者トークン |
| `POST` | `/api/rooms/:id/stop` | 進行中のターンを中断 | 参加者トークン |
| `POST` | `/api/admin/rooms` | 部屋作成 | 管理トークン |
| `DELETE` | `/api/admin/rooms/:id` | 部屋削除 | 管理トークン |
| `GET` | `/api/admin/rooms/:id/export` | ログのエクスポート | 管理トークン |

リアルタイム配信はSSEで足りる。WebSocketは不要（クライアントからの送信はPOSTで済むため）。

### SSEで流すイベント

```
event: message      // 新しい発言（user/system）
event: agent_start  // エージェントが考え始めた
event: agent_delta  // 本文の差分
event: agent_status // ツール実行中の状態表示（日本語に変換済み）
event: agent_end    // 完了。確定した本文を含む
event: presence     // 参加者の増減
```

---

## 8. Hermesへのリクエスト組み立て

```ts
POST http://127.0.0.1:8642/v1/chat/completions
Authorization: Bearer ${HERMES_API_KEY}

{
  "model": "kids",
  "stream": true,
  "messages": [
    { "role": "system", "content": "<部屋用のsystem prompt>" },
    { "role": "user", "content": "たろう: このプログラムなんでうごかないの？" },
    { "role": "user", "content": "はなこ: わたしもおなじところでつまった" },
    { "role": "user", "content": [
        { "type": "text", "text": "たろう: @ヘルメス これ見て" },
        { "type": "image_url", "image_url": { "url": "data:image/png;base64,..." } }
    ]}
  ]
}
```

実装上の注意。

- 履歴は直近N件（既定60件）に絞る。それ以前は要約して1つのsystemメッセージに畳む
- ストリームのチャンクにはツール進捗が混ざる。本文とツール進捗を分離して、前者を `agent_delta`、後者を `agent_status` に振り分ける
- 1ターンのタイムアウトは長め（Hermesはツールを何度も回すので2〜3分かかることがある）。ただし上限は必ず設ける（既定5分）
- Hermesが落ちている・キーが違うときは、子どもに見える言葉で「いまヘルメスとお話できないみたい」と返し、詳細はサーバーログに残す

### 8.1 LLMプロバイダはさくらのAI Engine

HermesのモデルプロバイダにはさくらのAI Engineを使う。OpenAI互換で、エンドポイントは `https://api.ai.sakura.ad.jp/v1`、認証はコントロールパネルで発行するアカウントトークン。

この選択の利点は性能ではなく所在にある。推論も通信も国内で完結し、送ったデータがモデルの学習に使われない。子どもの発言を扱う場では、これは保護者に説明できる材料になる。

一方で、3つの制約がそのまま設計に効く。

**ツール呼び出しの対応はモデルごとに違う。** Hermesはfunction callingを前提にしたエージェントで、ここが効かないと何も成立しない。AI Engineはツール連携対応をうたっているが、モデル単位では非対応のものもある（Kimi-K2.5はツール呼び出し非対応との報告がある）。gpt-oss-120bやQwen3系で実際に叩いて確かめてから、モデルを固定すること。

**画像入力は現時点で非対応との注記がある。** そうであれば、5.4の「画像はエージェントに渡す」は成り立たず、画像も部屋内での共有表示だけになる。検証の結果しだいで5.4を書き換えること。

**無料枠は月3,000リクエスト。** 月1回の利用なら枠内に収まる可能性はあるが、余裕はない。子ども10人が5〜10ターンずつ話すと50〜100ターン、エージェントは1ターンでツールを何度も回すので、1ターンあたり5〜15リクエストとして250〜1,500リクエスト（いずれも概算）。ここに開発中の試行分が同じ月に乗る。無料プランは枠を超えるとレート制限がかかり、イベント本番の途中で止まると取り返しがつかないので、**従量課金プランにしたうえで、BFF側のターン上限（11章）でコストを抑える**構成にする。

```yaml
# ~/.hermes/profiles/kids/config.yaml（キー名は実装時にドキュメントで確認）
provider: openai_compatible
base_url: https://api.ai.sakura.ad.jp/v1
model: <検証で選んだモデル>
```

### 部屋用 system prompt の骨子

```
あなたは子どもたちの集まる部屋にいるヘルメスです。
- 小学生にもわかる言葉で、短めに話します
- この部屋には複数の子どもがいます。発言は「なまえ: 本文」の形で届きます
- 返事をするときは、誰に向けた返事かわかるように名前を呼びます
- 答えをすぐ全部言わず、まず一緒に考えるヒントを出します
- 個人情報（住所・学校名・電話番号）を聞かれても答えず、聞き出そうともしません
- こわい話、暴力的な話、大人向けの話題にはのりません
```

---

## 9. 画面

3画面しかない。

1. **入室画面** `/r/{roomId}` — 部屋名、名前の入力、（あれば）合言葉、「はいる」ボタン、記録される旨の注意書き
2. **チャット画面** — タイムライン、入力欄、「AIにきく」ボタン、添付ボタン、参加者一覧、「とめる」ボタン
3. **管理画面** `/admin` — 部屋の一覧と作成フォーム

UI上の配慮。

- 文字は大きめ（本文16px以上）、ボタンは押しやすく
- 漢字にふりがなは振らない代わりに、UI文言をひらがな寄りにする
- 自分の発言と他の子の発言と、エージェントの発言を色と位置で明確に分ける
- エージェントが考えている間は必ず何か動いているものを見せる（待ち時間が長いため）
- スマホとタブレットを主対象にしたレイアウトにする

---

## 10. 技術スタック

| 層 | 選択 | 補足 |
|---|---|---|
| フロント | Vite + React + TypeScript | `react-development` スキルの方針に従う。feature-based構成、サーバー状態はTanStack Query、SSEはカスタムフック（`useRoomStream`）に隔離 |
| BFF | Node.js 22 + TypeScript + Hono | SSEとファイルアップロードが素直。1プロセスで静的配信も兼ねる |
| DB | SQLite（better-sqlite3） | |
| 配信 | 既存クラウドサーバー上のリバースプロキシでHTTPS終端 | |
| コンテナ | フロント＋BFFで1コンテナ。Hermesは別プロセス/別コンテナ | |

### 10.1 サーバーリソース — 現行の1コア/1GBでは足りない

対象サーバーはさくらのクラウド、Ubuntu 24.04、1コア/1GB/ディスク20GB。ここにHermesとDockerとBFFを同居させるのは無理がある。

同居時のメモリ見積もり（実測ではなく、公式の推奨値と一般的な構成からの概算）:

| 要素 | 目安 |
|---|---|
| Ubuntu本体 | 150〜250MB |
| BFF（Node + SQLite） | 150〜250MB |
| Hermes本体（gateway + API server） | 400〜700MB |
| ツール実行用Dockerコンテナ | 512MB〜1GB |
| 合計 | 1.2〜2.2GB |

公式の推奨は2コア/8GB、コンテナだけで1GB前後（ブラウザ自動操作を使う場合は2〜4GB）。**4GBを目標、2GBを下限**と考える。

使うのは月1回。この頻度なら、**イベントのたびにプランを上げ下げする運用で十分**で、常時4GBを抱える必要はない。

1. **イベントのときだけプラン変更する（採用）** — さくらのクラウドはサーバーを停止した状態でCPU・メモリのプランを変更でき、料金は時間課金。普段は1コア/1GB、前日に2コア/4GBへ上げ、翌日戻す。ディスクと設定はそのまま引き継がれるので、停止・変更・起動の3操作で済む
2. **アーカイブから毎回作り直す（併用を検討）** — 作り込んだ状態をアーカイブ化しておき、イベントのたびに4GBのサーバーを作って終わったら削除する。Hermesの記憶もプロファイルも毎回まっさらになるので、3.1の「イベント単位で立てて畳む」とは相性がいい。BFFのSQLiteだけは別途退避が要る
3. **常時4GBに上げる** — 月1回の用途には過剰

1を基本とし、運用が固まったら2への移行を検討する。あわせて次を行う。

- スワップを2GB作る（ディスク20GBなら余裕がある）。OOM Killerでゲートウェイが落ちるより遅くなるほうがましな場面が多い
- Dockerイメージは20GBのディスクを圧迫する。`nikolaik/python-nodejs` 系で2GB前後を見込み、定期的に `docker system prune` する
- 1コアなので、エージェントがツールを回している間はBFFの応答が鈍る。BFFはHermes呼び出しでイベントループを塞がない作りにし、SSE配信が止まらないことを負荷をかけて確認する

---

## 11. 安全・運用

技術で防げることと、運用で防ぐことを分けて書く。

**技術で防ぐ**

- Docker隔離（4.1）
- BFFでのレート制限: 参加者ごと毎分3ターン、部屋ごと1日100ターン（既定値。管理画面で変更可）
- アップロードの拡張子allowlistとサイズ上限
- 部屋の有効期限と定員
- 全メッセージの保存

**運用で防ぐ**

- 大人が同じ部屋に入っている状態で使う
- イベントごとに部屋を作り、終わったら消す
- プロファイル（記憶とスキル）もイベント単位で作り直す。子どもたちの発言がHermesの長期記憶に残り続けることを前提にしない
- 終了後にログに目を通す

子どもが不適切なことを言う/引き出そうとするのを技術だけで止めるのは無理だという前提に立つ。簡易的なNGワードフィルタは入れるが、それは最後の網ではない。

---

## 12. 実装前に検証すること

以下はドキュメントから読み取れなかった。Claude Codeは実装に入る前に、小さなスクリプトで実際に叩いて確認すること。

1. **画像入力** — API Serverが `image_url` 形式のコンテンツブロックを受け付けるか。受け付けない場合の代替（ファイルとして保存しパスを渡す）を検討する
2. **ストリームの形式** — ツール進捗が具体的にどのチャンクとして来るか。本文との分離方法
3. **ツールセットの絞り込み** — `hermes-api-server` のツールを設定で減らせるか（`/docs/user-guide/features/tools` を参照）
4. **セッションの扱い** — API Server が複数の同時リクエストをどう捌くか。部屋をまたいだ状態の混線が起きないか
5. **アップロードファイルのコンテナへの受け渡し** — `container_persistent: false` 下での経路
6. **実メモリの消費** — 10.1の見積もりは概算。プラン変更後に `docker stats` と `free -m` で実測し、`container_memory` と同時実行数の上限を決め直すこと
7. **さくらのAI Engineのツール呼び出し** — 候補モデルでfunction callingが実際に動くか。ここが最初の関門で、外れるとHermesを使う前提が崩れる
8. **さくらのAI Engineの画像入力** — 対応していなければ5.4を書き換える
9. **1ターンあたりのリクエスト数** — 実測して、無料枠/従量課金の見積もりとBFFのターン上限を決める

サーバーの条件（さくらのクラウド / Ubuntu 24.04 / 1コア・1GB・20GB）は判明済み。そのままでは動かないため、10.1の方針でプランを上げてから着手する。

---

## 13. 進め方

第1段階から順に、動くものを作って確かめながら進める。

1. **疎通** — BFFからHermes API Serverを叩いて、1往復の会話がCLIで通ることを確認（12.の検証もここで済ませる）
2. **1人用チャット** — 部屋の概念なし、SSEストリーミングだけ動かす
3. **部屋と参加** — 部屋の作成/入室/複数人の同時表示。エージェントはまだ絡めない
4. **合流** — 部屋の履歴からHermesへのリクエストを組み立て、呼びかけ時に返答させる
5. **添付** — 画像のみ
6. **管理画面とレート制限**
7. **子ども向けの文言とデザインの調整** — ここは実際に子どもに触ってもらってから直す部分が多いので、時間を取っておく

---

## 付録: 参照したドキュメント

- Messaging Gateway 全体: https://hermes-agent.nousresearch.com/docs/user-guide/messaging/
- API Server の設定と使い方（Open WebUI連携ページが実質的なAPIサーバーの解説）: https://hermes-agent.nousresearch.com/docs/user-guide/messaging/open-webui
- セキュリティモデル: https://hermes-agent.nousresearch.com/docs/user-guide/security
- ツールとツールセット: https://hermes-agent.nousresearch.com/docs/user-guide/features/tools
- 設定リファレンス: https://hermes-agent.nousresearch.com/docs/user-guide/configuration
- プロファイル: https://hermes-agent.nousresearch.com/docs/user-guide/profiles
