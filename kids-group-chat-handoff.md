# 子ども向け AI参加型グループチャット — 設計ハンドオフ

作成日: 2026-09-12 / 実装は Claude Code で行う前提のドキュメント。
リポジトリ名（仮）: `kids-group-chat`

前版（`hermes-kids-chat-handoff.md`）はHermes Agentを組み込む前提だった。目的を「複数人がひとつのAIとやり取りする場」と確認したうえで、Hermesを外す構成に作り直したのがこの版。

---

## 1. 何を作るか

子どもたちが、URLひとつで入れるチャットの部屋。その部屋に、AIがひとりの参加者として同席する。Discordなどのチャットツールを使えない子どもにも触ってもらえるようにするのが目的。

満たすべき要件。

- 基本のUIはチャット
- 複数ユーザーが同じ部屋に参加できる
- アカウント不要
- URLを知っている人しか入れない
- 部屋URLの作成・削除ができる
- 入室時に名前を入力する
- 画像などのファイル添付ができる

使うのは月1回程度。常時稼働のサービスではない。

---

## 2. 構成

```
 子どものブラウザ
      │  HTTPS
      ▼
 ┌──────────────────────────────────┐
 │ BFF (Node + TypeScript)          │  さくらのクラウド
 │  ・部屋と参加者の管理             │  Ubuntu 24.04
 │  ・会話ログの保持（SQLite）       │  1コア / 1GB / 20GB
 │  ・添付ファイルの保管             │
 │  ・レート制限                    │
 │  ・APIキーの保持                 │
 │  ・静的ファイルの配信             │
 └──────────────────────────────────┘
      │  HTTPS
      ▼
 さくらのAI Engine (https://api.ai.sakura.ad.jp/v1)
```

サーバーは1台。フロントの静的ファイルもBFFが配る。

### 2.1 Hermes Agent を使わない判断

Hermes Agentを組み込む前提で検討を進めたが、次の理由で外した。

**目的に対して機能が過剰。** Hermesの価値は、自律的にコマンドを実行すること、経験からスキルを蓄積すること、20以上のメッセージングプラットフォームに同時接続することにある。今回の目的は「複数人がひとつのAIと会話する」ことで、この3つはどれも使わない。

**重い。** Hermes本体で400〜700MB、ツール実行を隔離するDockerコンテナで512MB〜1GB。1コア/1GBのサーバーには載らず、プラン変更か2台目のサーバーが必要になる。BFFだけなら150〜250MBで収まる。

**信頼モデルが合わない。** Hermesは公式に「操作者は1人」を前提としており、呼び出し元はすべて同じ信頼度で扱われる。マルチユーザーの分離はOS/ホスト側でやること、と明記されている。URLを知る子ども全員がterminalツール経由でサーバーにコマンドを打てる状態になるため、隔離が必須になる。AI Engineを直接叩く構成なら、そもそも実行できるものが何もない。

**安くなる。** Hermesはエージェントとして1ターンにツール呼び出しを何度も回すので、1ターンあたり5〜15リクエスト（概算）を消費する。直接叩けば1ターン1リクエスト。さくらのAI Engineの無料枠は月3,000リクエストなので、月1回のイベントなら無料枠に収まる見込みが立つ。

**捨てるもの。** セッションをまたぐ永続メモリと、自律的なWeb検索。前者は必要なら自前で作れる（4.4）。後者は今回の目的には要らない。

---

## 3. 部屋でのAIの振る舞い

### 3.1 文脈は全員分、返答は呼びかけ時のみ

部屋の会話はひとつ。AIは全員の発言を読むが、返すのは呼びかけられたときだけにする。

参加者ごとに会話を分ける案は採らない。互いの質問とAIの答えが見えることが、この場のいちばんの価値になるため。一方で、5人も10人もいる部屋で全発言に返すと会話が読めない速さで流れる。だから履歴には全員の発言を入れ、発火だけ絞る。

呼びかけの判定は2系統。

1. 入力欄の横の「AIにきく」ボタン（既定の導線）。`@` の入力は低学年には確実でないので、ボタンを主役にする
2. 本文が `@AI` などのメンションで始まる場合

部屋の設定で「毎回返す」モードにも切り替えられるようにする。少人数のときはこちらが自然になる。

### 3.2 発言者の区別

AIに送る履歴では、発言者を本文の先頭に付ける。

```
user: たろう: このプログラムなんでうごかないの？
user: はなこ: わたしもおなじところでつまった
user: たろう: @AI おしえて
```

OpenAI互換APIの `name` フィールドはモデルによって扱いが違うので、本文に埋めるほうが確実。system promptで「複数の子どもが同じ部屋にいる」「発言は名前付きで届く」「返すときは誰に向けた返事か分かるように名前を呼ぶ」と説明しておく。

### 3.3 会話が伸びたときの扱い

毎ターン履歴を丸ごと送るので、長くなるほどトークンが増える。

- 直近60件をそのまま送る
- それ以前は要約して1つのsystemメッセージに畳む。要約の生成もAI Engineに投げる
- 部屋の想定（2時間・10人程度）なら、要約に入る前に終わる公算が高い。最初は上限に達したときだけ動く安全弁として実装すればよい

### 3.4 回をまたぐ記憶（第2段階）

必要になったら、部屋の終了時に会話の要約を作って保存し、次回の部屋のsystem promptに差し込む。「先月ロボットの話をしていたね」と返せるようになる。

子どもの発言を回をまたいで保持することになるので、入れるなら保護者への説明とセットにする。第1段階では作らない。

---

## 4. 機能仕様

### 4.1 部屋

| 項目 | 内容 |
|---|---|
| 作成 | 管理画面から。部屋名、有効期限、定員、合言葉の有無、返答モードを指定 |
| URL | `/r/{roomId}` |
| 削除 | 管理画面から。削除時は会話ログをJSONでエクスポートしてから論理削除 |
| 期限切れ | 入室不可。入室中の人には「この部屋はおわりました」を表示 |

URLの秘匿だけに頼らない。子どもがURLをそのまま外に貼る可能性は普通にある。

- 部屋IDは推測不能な22文字程度のランダム文字列（base62）
- 有効期限を持たせる（既定4時間、管理画面で延長可）
- 任意で4桁の合言葉を設定できる（当日その場で口頭で伝える運用）
- `Referrer-Policy: no-referrer` と `X-Robots-Tag: noindex` を付ける
- 定員を設ける（既定20人）

### 4.2 入室

名前を入力して入る。パスワードもメールもなし。

- 名前は部屋内で一意。重複したら再入力させる
- 入室時にBFFが参加者トークンを発行し、HttpOnly cookieに置く。完全ななりすまし防止ではないが、部屋の中で名前を乗っ取られることは防げる
- 名前は1〜12文字。改行とタブは除去
- 「ここでの会話は大人が見られるように記録されます」と入室画面に明示する

### 4.3 メッセージ

- 種別は `user` / `ai` / `system`（入退室、部屋の開始終了）の3つ
- AIの応答はストリーミングで全員の画面に流れる
- 誰でも押せる「とめる」ボタンを置く。押したら進行中の生成を打ち切る
- AIが応答している最中に別の子が発言した場合、その発言はタイムラインには即座に出し、次のターンの履歴に含める（生成中のリクエストには割り込まない）

### 4.4 添付ファイル

Hermesを外したことで、プロンプトに何を入れるかをBFFが完全に握れるようになった。ファイルの種類ごとに扱いを決める。

| 種類 | 扱い |
|---|---|
| テキスト系（txt / md / csv） | BFFが中身を読み、本文に添えてAIに渡す。上限は1万文字程度で打ち切り |
| PDF | サーバー側でテキスト抽出して同上。抽出できなければファイル名のみ |
| 画像 | タイムラインにサムネイル表示。AIに渡せるかはAI Engineの画像入力対応しだい（7.2）。非対応ならファイル名のみ伝える |
| その他 | 部屋の中で共有表示するだけ |

上限10MB、拡張子はallowlist方式。実行可能形式は受け付けない。

### 4.5 管理画面

`/admin` に置き、環境変数の管理トークンで入る（子ども用の部屋URLとは別系統）。

- 部屋の作成・一覧・削除
- 部屋ごとの会話ログ閲覧とエクスポート
- 参加者の強制退出
- 部屋ごとのリクエスト使用量表示

---

## 5. データモデル

SQLiteで持つ。

```sql
rooms(
  id TEXT PRIMARY KEY,          -- 22文字のランダム
  name TEXT NOT NULL,
  passcode_hash TEXT,           -- NULL可
  reply_mode TEXT NOT NULL,     -- 'mention' | 'always'
  capacity INTEGER NOT NULL,
  turn_limit INTEGER NOT NULL,
  turns_used INTEGER NOT NULL,
  expires_at TEXT NOT NULL,
  deleted_at TEXT,
  created_at TEXT NOT NULL
)

participants(
  id TEXT PRIMARY KEY,
  room_id TEXT NOT NULL,
  display_name TEXT NOT NULL,
  token_hash TEXT NOT NULL,
  joined_at TEXT NOT NULL,
  left_at TEXT,
  UNIQUE(room_id, display_name)
)

messages(
  id TEXT PRIMARY KEY,
  room_id TEXT NOT NULL,
  kind TEXT NOT NULL,           -- 'user' | 'ai' | 'system'
  participant_id TEXT,          -- kind='user' のとき
  body TEXT NOT NULL,
  created_at TEXT NOT NULL
)

attachments(
  id TEXT PRIMARY KEY,
  message_id TEXT NOT NULL,
  original_name TEXT NOT NULL,
  mime_type TEXT NOT NULL,
  size INTEGER NOT NULL,
  stored_path TEXT NOT NULL,
  extracted_text TEXT           -- テキスト/PDFから抽出した内容
)
```

---

## 6. BFFのAPI

| メソッド | パス | 用途 | 認証 |
|---|---|---|---|
| `GET` | `/api/rooms/:id` | 部屋の存在確認（名前・期限・合言葉要否） | なし |
| `POST` | `/api/rooms/:id/join` | 名前を送って入室、参加者トークン発行 | なし |
| `GET` | `/api/rooms/:id/messages` | 入室直後の履歴取得 | 参加者トークン |
| `GET` | `/api/rooms/:id/stream` | SSE。新着とAIのストリームを配信 | 参加者トークン |
| `POST` | `/api/rooms/:id/messages` | 発言（`askAi: boolean` を含む） | 参加者トークン |
| `POST` | `/api/rooms/:id/attachments` | ファイルアップロード | 参加者トークン |
| `POST` | `/api/rooms/:id/stop` | 生成の中断 | 参加者トークン |
| `POST` | `/api/admin/rooms` | 部屋作成 | 管理トークン |
| `DELETE` | `/api/admin/rooms/:id` | 部屋削除 | 管理トークン |
| `GET` | `/api/admin/rooms/:id/export` | ログのエクスポート | 管理トークン |

リアルタイム配信はSSEで足りる。クライアントからの送信はPOSTで済むのでWebSocketは要らない。

### SSEで流すイベント

```
event: message    // 新しい発言（user/system）
event: ai_start   // AIが応答を始めた
event: ai_delta   // 本文の差分
event: ai_end     // 完了。確定した本文を含む
event: ai_error   // 失敗
event: presence   // 参加者の増減
```

AIの応答は、1本のAPIリクエストのストリームをBFFが受け、部屋の全員のSSE接続にファンアウトする。生成中に入室した子にも、途中から流れているものが見えるようにする。

---

## 7. さくらのAI Engineへのリクエスト

### 7.1 組み立て

```
POST https://api.ai.sakura.ad.jp/v1/chat/completions
Authorization: Bearer ${SAKURA_AI_TOKEN}

{
  "model": "<選定したモデル>",
  "stream": true,
  "messages": [
    { "role": "system", "content": "<部屋用のsystem prompt>" },
    { "role": "user", "content": "たろう: このプログラムなんでうごかないの？" },
    { "role": "user", "content": "はなこ: わたしもおなじところでつまった" },
    { "role": "user", "content": "たろう: @AI おしえて" }
  ]
}
```

実装上の注意。

- APIキーはBFFだけが持つ。ブラウザには絶対に出さない
- タイムアウトは60秒。エラー時は子どもに見える言葉で「いまAIとお話できないみたい」と返し、詳細はサーバーログへ
- AI Engineが落ちている・枠を使い切った場合も同じ扱い。部屋の会話自体は続けられるようにする（子ども同士のチャットは生きたまま）

### 7.2 モデル選定

会話だけなので、function callingは要らない。選定の観点は日本語の自然さと応答速度、それとコンテキスト長。gpt-oss-120bで128Kトークン、Qwen3系で256Kあるので、部屋の履歴は余裕で収まる。

画像入力については、AI Engine側に「現時点では画像入力に対応していない」旨の注記がある。候補モデルで実際に試して、4.4の画像の扱いを確定させること。

### 7.3 コスト

1ターン1リクエスト。子ども10人が5〜10ターンずつなら、イベント1回あたり50〜100リクエスト（概算）。無料枠の月3,000リクエストに対して十分な余裕がある。開発中の試行分を足しても収まる見込み。

ただし無料プランは枠を超えるとレート制限がかかる。本番の途中で止まるのが最悪なので、イベント月の残量は事前に確認する。心配なら従量課金プランにしておく。

### 7.4 部屋用 system prompt の骨子

```
あなたは子どもたちの集まるチャットの部屋にいる、AIの仲間です。
- 小学生にもわかる言葉で、短めに話します
- この部屋には複数の子どもがいます。発言は「なまえ: 本文」の形で届きます
- 返事をするときは、誰に向けた返事かわかるように名前を呼びます
- 答えをすぐ全部言わず、まず一緒に考えるヒントを出します
- 個人情報（住所・学校名・電話番号）を聞かれても答えず、聞き出そうともしません
- こわい話、暴力的な話、大人向けの話題にはのりません
```

プロンプトで書いた禁止事項は破られうる前提で運用する（10章）。

---

## 8. 画面

3画面。

1. **入室画面** `/r/{roomId}` — 部屋名、名前の入力、（あれば）合言葉、「はいる」ボタン、記録される旨の注意書き
2. **チャット画面** — タイムライン、入力欄、「AIにきく」ボタン、添付ボタン、参加者一覧、「とめる」ボタン
3. **管理画面** `/admin` — 部屋の一覧と作成フォーム

UI上の配慮。

- 本文16px以上、ボタンは押しやすく
- UI文言はひらがな寄りにする
- 自分の発言・他の子の発言・AIの発言を、色と位置で明確に分ける
- AIが応答している間は何か動いているものを見せる
- スマホとタブレットを主対象にしたレイアウト

---

## 9. 技術スタックとリソース

| 層 | 選択 | 補足 |
|---|---|---|
| フロント | Vite + React + TypeScript | `react-development` スキルの方針に従う。feature-based構成、SSEは `useRoomStream` カスタムフックに隔離 |
| BFF | Node.js 22 + TypeScript + Hono | SSEとファイルアップロードが素直。静的配信も兼ねる |
| DB | SQLite（better-sqlite3） | |
| 配信 | Caddy か nginx でHTTPS終端 | |

メモリの見込み（概算）。

| 要素 | 目安 |
|---|---|
| Ubuntu本体 | 150〜250MB |
| BFF（Node + SQLite） | 150〜250MB |
| 合計 | 300〜500MB |

1コア/1GBに収まる。プラン変更もDockerも2台目のサーバーも要らない。念のためスワップを2GB作っておく。

1コアである点だけ注意。AI Engineへのリクエストはネットワーク待ちが大半なのでCPUは食わないが、SSEのファンアウトとファイルのテキスト抽出で詰まらせないこと。特にPDF抽出は同期処理にしない。

---

## 10. 安全・運用

コマンド実行の経路がなくなったので、残るリスクは内容と使われ方の面に絞られる。

**技術で防ぐ**

- レート制限: 参加者ごと毎分3ターン、部屋ごと1日100ターン（既定値、管理画面で変更可）
- アップロードの拡張子allowlistとサイズ上限
- 部屋の有効期限と定員
- 全メッセージの保存
- 簡易的なNGワードフィルタ（最後の網ではない）

**運用で防ぐ**

- 大人が同じ部屋に入っている状態で使う
- イベントごとに部屋を作り、終わったら消す
- 終了後にログに目を通す

子どもが不適切なことを言う、あるいはAIから引き出そうとするのを技術だけで止めるのは無理だという前提に立つ。system promptは体験の調整であって安全装置ではない。

---

## 11. 実装前に検証すること

1. **ストリーミング** — AI Engineの `stream: true` でチャンクが期待どおり流れるか。SSEへの変換
2. **画像入力** — 候補モデルが画像を受けるか。結果しだいで4.4を書き換える
3. **モデルの日本語** — 子ども向けの平易な日本語で、短めに返せるか。複数モデルを同じプロンプトで比較する
4. **名前付き履歴の理解** — 「なまえ: 本文」形式で複数人の会話を渡したとき、誰に向けて返すべきかを取り違えないか。ここはモデルによって差が出る可能性がある
5. **無料枠の残量確認方法** — コントロールパネルで当月の使用量をどう見るか

---

## 12. 進め方

1. **疎通** — BFFからAI Engineを叩き、ストリーミングで応答が返ることをCLIで確認（11の1〜4をここで済ませる）
2. **1人用チャット** — 部屋の概念なし。SSEで1対1の会話が流れるところまで
3. **部屋と参加** — 部屋の作成/入室/複数人の同時表示。AIはまだ絡めない
4. **合流** — 部屋の履歴からリクエストを組み立て、呼びかけ時にAIが返す
5. **添付** — テキスト系とPDFのテキスト抽出、画像のサムネイル表示
6. **管理画面とレート制限**
7. **子ども向けの文言とデザインの調整** — 実際に子どもに触ってもらってから直す部分が多いので、時間を取っておく

3までは外部APIに依存しないので、無料枠を消費せずに進められる。

---

## 付録: 参照したドキュメント

- さくらのAI Engine（サービス概要）: https://www.sakura.ad.jp/aipf/ai-engine/
- さくらのAI Engine（クラウドマニュアル）: https://manual.sakura.ad.jp/cloud/manual-ai-engine.html
- エンドポイント: `https://api.ai.sakura.ad.jp/v1`（OpenAI互換 Chat Completions）
